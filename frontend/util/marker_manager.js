class MarkerManager {
    constructor(map) {
        this.amp = map;
        this.markers = {};
    }

    updateMarkers(businesses) {
        console.log("time to update")
    }
}

export default MarkerManager;